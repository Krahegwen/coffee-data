{
  "name": "@coffee/web",
  "version": "0.1.19",
  "private": true,
  "type": "module",
  "description": "Bitácora de café: app instalable",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "echo \"sin tests todavía\" && exit 0"
  },
  "dependencies": {
    "nuxt": "^4.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vite-pwa/nuxt": "^1.0.0"
  }
}
